const Item = require('./items.models');
const User = require('./users.models');
const Role = require('./roles.models');
const Menu = require('./menus.models');
const Bill = require('./bills.models');
const AuditTrail = require('./auditTrails.models');
const Login = require('./login.models');

module.exports = {
  Item,
  User,
  Role,
  Menu,
  Bill,
  AuditTrail,
  Login
}